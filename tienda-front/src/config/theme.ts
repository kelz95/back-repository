import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#57B089",
    },
    secondary: {
      main: "#F3C74F",
    },
    error: {
      main: "#E6595A",
    },
  },
});

export default theme;
